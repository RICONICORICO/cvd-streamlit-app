# CVD XGBoost Streamlit Prototype

This Streamlit app uses a reduced XGBoost model pipeline to return a binary cardiovascular disease prediction: `YES` or `NO`.

Required files:

- `app.py`
- `requirements.txt`
- `reduced_xgb_pipeline.pkl`
- `model_metadata.pkl`
